/**
 * Single source of truth for the displayed app version.
 * Keep in sync with the `version` fields in /package.json, frontend/package.json,
 * and `#define AppVersion` in /installer.iss when cutting a release (see CHANGELOG.md).
 */
export const APP_VERSION = "1.1.64";
